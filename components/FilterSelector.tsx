import { FilterSelectorProps } from "@/types";
import React from "react";

const FilterSelector = ({ title }: FilterSelectorProps) => {
  return <div>FilterSelector</div>;
};

export default FilterSelector;
